import type { Meta, StoryObj } from "@storybook/react";

const meta: Meta = {
    title: "Atoms/Spacing",
    parameters: {
        layout: "centered",
    },
    tags: ["autodocs"]
};

export defaul meta;